#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int m, n;
    scanf("%d %d", &m, &n);
    char arr[m][n];
    scanf("\n");
    
    int i = 0, j = 0;
    
    for(i = 0; i < m; i++)
    {
        for(j = 0; j < n; j++)
        {
            scanf("%c", &arr[i][j]);
            /* После последнего символа в строке не будет пробела,
            поэтому приходится проверять */
            if(j != n - 1)
            {
                scanf(" ");
            }
        }
        // Аналогично с переносом строки
        if(i != m - 1)
        {
            scanf("\n");
        }
    }
    
    char rot[n][m];
    
    printf("\n");
    for(int k = 0; k < n; k++)
    {
        for(int l = 0; l < m; l++)
        {
            rot[k][l] = arr[m - l - 1][k];
            printf("%c ", rot[k][l]);
        }
        printf("\n");
    }
    
    return 0;
}
